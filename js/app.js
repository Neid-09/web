function verMas(seccion) {
    alert("Mostrando más información de " + seccion);
}
function iniciarJuego() {
    if (confirm("¿Estás listo para el desafío?")) {
        window.location.href = "juego.html";
    }
}